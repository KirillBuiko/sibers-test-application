export const INDEXED_DB_NAME = "chat-db";
export const INDEXED_DB_VER = 1;
export const BROADCAST_CHANNEL = "chat-notifications";

export enum IndexedDbStore {
    MESSAGES = "messages",
    CHANNELS = "channels",
    USERS = "users"
}